{% extends 'base.html.twig' %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('bower_components/font-awesome/css/all.css') }}">
<style>
    .card-img-top {
        width: 100%;
        height: 30vh;
        object-fit: cover;
    }

</style>
{% endblock %}

{% block title %}babyStuff | {{ categorie.slug}}{% endblock %}

{% block body %}

<h1 class="marque-title">{{ categorie.nom }} </h1>
{# <div class="jumbotron">
    <div class="container">
        {{ form_start(form) }}
        <div class="form-row">
            <div class="col">
                {{ form_row(form.price) }}
            </div>

            <div class="col">
                {{ form_row(form.name) }}
            </div>

            <div class="col">
                <button class="btn btn-primary">Rechercher</button>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
</div> #}
<section class="marques">
    <div class="containner">
        <div class="row">
            {% for produit in pagination %}
            <div class="col-sm-3 card marque-products">
                <div class="card-body">
                    <div>
                        <a href="{{ path('single_product', {'slug': produit.slug}) }}"><img
                                src="{{ asset('/brows_file/' ~ produit.image) }}" class="card-img-top" alt=""></a>
                        <p class="card-text">{{ produit.nom }}</p>
                        <a href="#" class="add-to-favourite"></a>
                    </div>
                    <div class="card-title product-title"></div>
                    <a href="{{ path('single_product', {'slug': produit.slug}) }}" class="btn btn-primary add-to-cart">
                        Check out!!</a>
                </div>
            </div>
            <!--END SINGLE PRODUCTS-->
            {% endfor %}
        </div>
        <div class="navigation pagination-sm">
            {{ knp_pagination_render(pagination) }}
            <div>
            </div>
</section>
{% endblock %}
