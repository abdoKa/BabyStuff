<style>
  @media (min-width: 780px) {
    .animate {
      animation-duration: 0.3s;
      -webkit-animation-duration: 0.3s;
      animation-fill-mode: both;
      -webkit-animation-fill-mode: both;
    }
  }

  @keyframes slideIn {
    0% {
      transform: translateY(1rem);
      opacity: 0;
    }

    100% {
      transform: translateY(0rem);
      opacity: 1;
    }

    0% {
      transform: translateY(1rem);
      opacity: 0;
    }
  }

  @-webkit-keyframes slideIn {
    0% {
      -webkit-transform: transform;
      -webkit-opacity: 0;
    }

    100% {
      -webkit-transform: translateY(0);
      -webkit-opacity: 1;
    }

    0% {
      -webkit-transform: translateY(1rem);
      -webkit-opacity: 0;
    }
  }

  .slideIn {
    -webkit-animation-name: slideIn;
    animation-name: slideIn;
  }

  .btn-group {
    right: -120px;
  }

</style>

<div class="container">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top" id="shadow">
    <a class="navbar-brand" href="{{ path('home') }}">
      <i class="fas fa-baby-carriage">
      </i> BabyStuff<small><sub>Demo</sub></small></a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
      aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>

    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav sm-auto">


        <li class="nav-item">
          <a class=" nav-link scroll" href="{{ path('marques') }}"> Marques </a>
        </li>

        <li class="nav-item dropdown" id="navbarSupportedContent">
          <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
            aria-expanded="false">Nos Produits</a>
          <div class="dropdown-menu  animate slideIn" aria-labelledby="navbarDropdown">
            {% for categorie in categoriesMenu %}
            <a class="dropdown-item" href="{{ path('show_categorie', {'slug': categorie.slug }) }}">
              {{ categorie.nom }} </a>
            <div role="separator" class="dropdown-divider"></div>
            {% endfor %}
            <a class="dropdown-item" href="{{ path('show-all-categories') }}">tous les catégories</a>

          </div>
        </li>

        <li class="nav-item ">
          <a class="nav-link" href="{{ path('about') }}">A propos </a>
        </li>


      </ul>


      <ul class="navbar-nav ml-auto">
        <li "class=" dropdown"">

          <a class="nav-link" href="{{ path('shopping') }}"> Panier
            <i class="fas fa-shopping-basket">
              {% if cartDetails is defined and cartDetails is not null %}
              <span id="totalSum">{{cartDetails.totalsum}}</span> <small>MAD</small>
              {% else %}
              <span><small>0 MAD</small></span>
              {% endif %}

            </i>
          </a>
      </ul>
      <li class="navbar-nav " style="margin:0 -20px 0 30px;">
        <small><a class="nav-link" href="#"><i class="far fa-heart"></i>
            <spane>Favorites</spane>
          </a></small>
      </li>
      {% if is_granted("ROLE_USER") %}

      <ul class="navbar-nav m-auto" id="profile-right">
        <div class="btn-group">
          <li class="nav-item dropdown" id="navbarSupportedContent">
            <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
              aria-expanded="true">
              <spane>Slaut!</spane> {{ app.user.nom }}
            </a>
            <div class="dropdown-menu  animate slideIn" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="{{ path('user_account') }}">
                <i class="far fa-user-circle"></i>
                <spane> Mon Espace </spane>
              </a>
              <a class="dropdown-item" href="{{ path('user_favorite') }}"><i class="fas fa-heart"></i>
                <spane> Favorites </spane>
              </a>
              <a class="dropdown-item" href="{{ path('user_profile_edit') }}"><i class="fas fa-cogs"></i>
                <spane> Paramètre </spane>
              </a>
              {% if is_granted("ROLE_ADMIN") %}
              <a class="dropdown-item" href="{{ path('admin') }}"><i class="fas fa-desktop"></i>
                <spane> Back-Office </spane>
              </a>
              {% endif %}
              <a class="dropdown-item" href="{{ path('logout') }}"><i class="fas fa-sign-out-alt"></i>
                <spane> Deconnexion </spane>
              </a>
            </div>

      </ul>


      {% else %}

      <ul class="navbar-nav ml-auto">
        <li class="navbar-nav ml-auto">
          <a class="nav-link" href="{{ path('security_login') }}"><i class="fas fa-sign-in-alt"></i>
            <spane> <small>Se connecter</small> </spane>
          </a>
        </li>
      </ul>

      {% endif %}
    </div>
  </nav>

</div>
