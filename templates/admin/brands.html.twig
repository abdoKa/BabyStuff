  {% extends "admin/base-admin.html.twig" %}
  {% block stylesheets %}

  <style>
      .image_B {
          width: 50%;

      }

      td,
      th {
          text-align: center;
      }
  </style>
  {% endblock %}

  {% block head %}
  <div class="container">
      <div class="row">
          <div class="col-lg-12">
              <h1 class="Welcom-title"> list des fournisseur </h1>
          </div>
          {# / part backend client settings #}
      </div>
      {# / part backend client settings #}
  </div>
  <div class="row">

      <div class="col-sm-3">

          {% include 'admin/menu.html.twig' %}
      </div>
      <div class="col-sm-9">
          <div>
              <i class="fas fa-plus"></i> <a href="{{ path('new_product') }}">
                  <spane>Nouvelle Fournisseur</spane>
              </a>
          </div>
          <table class="table">
              <thead>
                  <tr bgcolor="d7f7f5">
                      <th scope="col">N°</th>
                      <th scope="col">Fournisseur</th>
                      <th scope="col">Descriprtion</th>
                      <th scope="col">image</th>
                      <th scope="col-md-2">actes</th>
                  </tr>
              </thead>
              <tbody>
                  {% for fournisseur in pagination %}
                  <tr>
                      <th scope="row">{{ fournisseur.id }}</th>
                      <td>{{ fournisseur.nom }}</td>
                      <td>{{ fournisseur.description }}</td>
                      <td><img src="{{ asset('/brows_file/' ~ fournisseur.image) }}"></td>
                      <td>
                          <a href="#"><i class="fas fa-eye btn-light btn-sm"></i></a> <a href="#"><i
                                  class="fas fa-pencil-alt btn-light btn-sm"></i></a>

                      </td>
                  </tr>
                  {% endfor %}
              </tbody>
          </table>
      </div>
      </div>
        <div class="navigation pagination-sm" style="padding-left:1000px;">
          {{ knp_pagination_render(pagination) }}
          </div>
      {% endblock %}