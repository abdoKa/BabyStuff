  {% extends "admin/base-admin.html.twig" %}
  {% block stylesheets %}
  <style>
      .container {
          margin-top: 50px;
      }

  </style>

  {% endblock %}

  {% block head %}

  <div class="container">
      <div class="row">
          <div class="col-lg-12">
              <h1 class="Welcom-title"> list des Commandes </h1>
          </div>
          {# / part backend client settings #}
      </div>
      {# / part backend client settings #}
  </div>
  <div class="row">

      <div class="col-sm-3">

          {% include 'admin/menu.html.twig' %}
      </div>
      <div class="col-sm-9">
          <table class="table">
              <thead>
                  <tr bgcolor="d7f7f5">
                      <th scope="col">N°</th>
                      <th scope="col">Client</th>
                      <th scope="col">E-mail</th>
                      <th scope="col">Prix Totale</th>
                      <th scope="col">Date du commande</th>
                      <th scope="col-md-2">actes</th>
                  </tr>
              </thead>
              <tbody>

                  {% for order in pagination %}
                  <tr>
                      <th scope="row">{{order.id}}</th>
                      <td>{{ order.nomUtilisateur }} {{ order.PrenomUtilisateur }}</td>
                      <td>{{order.email}}</td>
                      <td>{{order.prixTotale}} <small>MAD</small></td>
                      <td>{{order.createdAt|date("m/d/Y H:i", "Europe/Paris") }}</td>
                      <td><a href="{{ path('order_detail',{'id':order.id}) }}"><i
                                  class="fas fa-eye btn-light btn-sm"></i></a></td>
                  </tr>
                  {% endfor %}
              </tbody>
          </table>
      </div>
  </div>
  </div>
  <div class="navigation pagination-sm">
      {{ knp_pagination_render(pagination) }}
  </div>
  {% endblock %}
